{
  "name": "yuescriptrunner",
  "displayName": "月Runner",
  "description": "In-editor Yuescript compilation assistant",
  "publisher": "MTLabs",
  "version": "0.0.10",
  "icon": "logo.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/MTadder/YueRunner.git"
  },
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Testing"
  ],
  "keywords": [
    "yuescript",
    "yue",
    "yue love",
    "yue compiler",
    "yue runner"
  ],
  "activationEvents": [
    "onLanguage:yuescript"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "月Runner Configuration",
      "properties": {
        "yuescriptrunner.useMinification": {
          "type": "boolean",
          "default": false,
          "description": "Use code minification during compilation?"
        },
        "yuescriptrunner.useSpacesInstead": {
          "type": "boolean",
          "default": false,
          "description": "Use spaces instead of tabs during compilation?"
        },
        "yuescriptrunner.reserveComments": {
          "type": "boolean",
          "default": false,
          "description": "Reserve comments before statements?"
        },
        "yuescriptrunner.writeLineNumbers": {
          "type": "boolean",
          "default": false,
          "description": "Write line numbers from the source code? (doesn't write files!)"
        },
        "yuescriptrunner.dumpCompileTime": {
          "type": "boolean",
          "default": false,
          "description": "Only dump compile time? (doesn't write files!)"
        },
        "yuescriptrunner.useImplicitReturn": {
          "type": "boolean",
          "default": true,
          "description": "Use implicit return at end of file?"
        },
        "yuescriptrunner.matchLineNumbers": {
          "type": "boolean",
          "default": false,
          "description": "Rewrite output to match original line numbers?"
        },
        "yuescriptrunner.loveExecutable": {
          "enum": [
						"love", "lovec"
					],
          "type": "string",
          "default": "lovec",
          "description": "Specify the LÖVE executable to use.",
          "title": "LÖVE executable"
        },
        "yuescriptrunner.targetLuaVersion": {
          "enum": [
						"5.1", "5.2", "5.3", "5.4"
					],
          "type": "string",
          "default": "5.4",
          "description": "Specify the Lua version that the compiler will target."
        }
      }
    },
    "commands": [
      {
        "command": "yuescriptrunner.compile",
        "title": "Compile this Yuescript"
      },
      {
        "command": "yuescriptrunner.compile_all",
        "title": "Compile all Yuescripts (in this directory)"
      },
      {
        "command": "yuescriptrunner.compile_all_and_make_love",
        "title": "Compile all Yuescripts & run LÖVE2D (in this directory)"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "yuescriptrunner.compile",
          "when": "editorLangId == yuescript"
        },
        {
          "command": "yuescriptrunner.compile_all",
          "when": "editorLangId == yuescript"
        },
        {
          "command": "yuescriptrunner.compile_all_and_make_love",
          "when": "editorLangId == yuescript"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.92.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "@typescript-eslint/eslint-plugin": "^7.14.1",
    "@typescript-eslint/parser": "^7.11.0",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}